#include <stdio.h>

int main()
{
    int a = 34, b = 41;

    if (a > 25 && b > 25)
    {
         printf("Both values are greater than 25\n");
    }
    else
    {
        printf("Both values are less than 25\n");
    }
    return 0;
}
